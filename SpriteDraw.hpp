#ifndef SPRITEDRAW_H
#define SPRITEDRAW_H

#include <SFML/Graphics.hpp>
#include <cstdlib>

class SpriteDraw{

public:
    SpriteDraw();
    ~SpriteDraw();

    void drawRandomCircle(sf::RenderWindow* l_Window);

private:


};
#endif // SPRITEDRAW_H
